var lang_en = false;
export default function changeLanguage(change){
    if(change === true){
        if(lang_en ? lang_en = false : lang_en = true);
        console.log(lang_en);
    }

    return lang_en;
};